@model IEnumerable<ProyectoNukeMapuPewmaMVC.Models.Ropa>
@{
    ViewData["Title"] = "Ropa";
}


<div class="text-central">
    <h1 class="display-4">Catálogo de Ropa</h1>
    <a asp-area=""  class="btn btn-success" asp-action="RopaCrear">Crear nueva</a>


    <table class="table"></table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Talla</th>
                <th>Descripción</th>
                <th>Precio</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
            @* ISAAC has acto de presencia aqui que me perdi :D ----

            @if (Model != null && Model.Any()) 
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.Talla</td>
                        <td>@item.Descripcion</td>
                        <td>@item.Precio</td>
                        <td>
                            <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-warning">Editar</a>
                            <a asp-action="Eliminar" asp-route-id="@item.Id" class="btn btn-danger">Eliminar</a>
                        </td>
                    </tr>
                }
            } 
            else
            {
                <tr>
                    <td colspan="5"><strong>No hay productos en la base de datos</strong></td>
                </tr>
            }
        </tbody>
        *@
    </table>


</div>
